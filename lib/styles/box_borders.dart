part of styles;

class BoxBorders{
    static final BorderRadius primaryBoxBorder = BorderRadius.circular(20);
     static final BorderRadius secondaryBoxBorder = BorderRadius.only(
                      topLeft: Radius.circular(50),
                      topRight: Radius.circular(50));
         static final BorderRadius teritaryBoxBorder = BorderRadius.circular(5);             
}